# Incidents API

Минималистичный API для учёта инцидентов (FastAPI + SQLAlchemy + SQLite).

## Запуск (локально)

1. Создать проект и виртуальное окружение (PyCharm сделает это автоматически)  
2. Установить зависимости:
```bash
pip install -r requirements.txt
